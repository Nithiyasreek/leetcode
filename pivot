class Solution {
    public int pivotInteger(int n) {
        int totalsum=n*(n+1)/2;
        int leftSum=0;
        for(int i=1;i<=n;i++){
            leftSum=leftSum+i;
            if(leftSum==totalsum-leftSum+i){
                return i;
            }
        }
       return -1; 
    }
}
